import React from 'react';
import { StoreType } from '../model/store.js';
export declare const GroupButton: (({ store }: {
    store: StoreType;
}) => React.JSX.Element) & {
    displayName: string;
};
