import React from 'react';
import { StoreType } from '../model/store.js';
type Props = {
    store: StoreType;
    components?: any;
};
export declare const ManyToolbar: (({ store, components }: Props) => React.JSX.Element) & {
    displayName: string;
};
export default ManyToolbar;
